package com.gi199.randomod.command;

import com.mojang.brigadier.Command;
import net.minecraft.server.command.ServerCommandSource;

class CommandClasses {
    Command<ServerCommandSource> command = context -> 0;
}
