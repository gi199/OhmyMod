pluginManagement {
    repositories {
        maven {
            name = 'Hanbings Maven Repository'
            url = 'https://repository.hanbings.io/proxy'

        }
        mavenCentral()
    }
    plugins {
        id 'org.gradle.toolchains.foojay-resolver-convention' version '0.8.0'
    }
}